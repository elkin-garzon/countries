name: test

on: [puhs]

jobs:
  build-and-test:
    run-on: ubuntu-latest
    steps:
      -uses: actions/checkount@v4
      -run: npm i
      -run: npm test
      -run: echo "test complete"
